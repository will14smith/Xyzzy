import React from 'react';

// this state shouldn't ever be displayed
// just including for completeness
export default class GameDealing extends React.Component {
  render() {
    return <div className="card">Dealing...</div>;
  }
}
